{
  "modid": "${project.artifactId}",
  "name": "${project.name}",
  "author_list": ["kiooeht"],
  "description": "Replace any texture. Dump all textures.",
  "version": "${project.version}",
  "sts_version": "${SlayTheSpire.version}",
  "mts_version": "${ModTheSpire.version}",
  "dependencies": ["basemod"]
}